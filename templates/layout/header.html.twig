<nav class="navbar navbar-expand-lg fixed-top navbar-light bg-light">
    <a class="navbar-brand font-title" href="{{ path('home') }}">
        <img src="{{ asset('images/popcorn.png') }}" width="25" height="25" class="d-inline-block align-top" alt="{{ project_title }} logo">
        {{ project_title }}
    </a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item {{ app.request.get('_route') == 'user.feed' ? 'active'}}">
                <a class="nav-link" href="{{ path('user.feed') }}">Feed</a>
            </li>
            <li class="nav-item {{ app.request.get('_route') == 'user.list' ? 'active'}}">
                <a class="nav-link" href="{{ path('user.list') }}">User</a>
            </li>
        </ul>
        <div class="my-2 my-lg-0 dropdown">
            <button class="btn btn-secondary" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fa fa-user-o"></i>
            </button>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                <a class="dropdown-item" href="{{ path('user.edit') }}">{{ app.user }}</a>
                <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Logout</a>
            </div>
        </div>
    </div>
</nav>
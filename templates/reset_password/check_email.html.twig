{% extends 'offline_base.html.twig' %}

{% block title %}{{ parent() }} | Email envoyé{% endblock %}

{% block body %}
    <div class="row full-page-y center-container custom-font-text custom-bold-500">
        <div class="col-10 col-sm-8 col-md-6 center-content security-form">
            <h1 class="h3 mb-2 text-center display-4 custom-font-text">{{ project_title }}</h1>
            <p>Un Email contenant un lien pour modifier ton mot de passe vient de partir. Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
            <p>Si tu ne reçois rien, vérifie tes spams ou <a href="{{ path('app_forgot_password_request') }}" class="underline">réessaie</a>.</p>
        </div>
    </div>
{% endblock %}
